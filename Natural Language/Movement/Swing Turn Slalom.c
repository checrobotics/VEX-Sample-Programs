/**************************\
|*  ROBOTC for Beginners  *|
|*  Swing Turn Slalom     *|
\**************************/

task main()
{
  robotType(recbot);  // We are using the recbot.

  swingTurn(left);    // Swing Turn left.
  wait(1.25);         // Wait 1.25 seconds.

  swingTurn(right);   // Swing Turn right.
  wait(1.25);         // Wait 1.25 seconds.

  swingTurn(left);    // Swing Turn left.
  wait(1.25);         // Wait 1.25 seconds.

  swingTurn(right);   // Swing Turn right.
  wait(1.25);         // Wait 1.25 seconds.

  swingTurn(left);    // Swing Turn left.
  wait(1.25);         // Wait 1.25 seconds.

  swingTurn(right);   // Swing Turn right.
  wait(1.25);         // Wait 1.25 seconds.
}
